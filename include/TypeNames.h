#ifndef TYPENAMES_H
#define TYPENAMES_H

#include <cstdint>

// This is used for variables in memory
// and accessing the registers
typedef uint8_t BYTE;

// This is used for the program counter
// and accessing ram
typedef uint16_t WORD;

// These dont have a use right now
typedef uint32_t DWORD;
typedef uint64_t QWORD;

#endif
